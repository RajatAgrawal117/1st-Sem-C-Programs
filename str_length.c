#include<stdio.h>
int main(){
int len = 0;
char str[387387];
puts("Enter the string");
gets(&str[len]);
while (s[len]!='\0')
{
    len ++;

}
printf("Length of string is : %d", len);
return 0;
}